"""
Lesson 1 In-Class Exercises (No Answers)
========================================

This file is intentionally scaffold-only.
Implement each TODO during class.
"""

# ========================
# Section 1: Variables
# ========================

# Exercise 1:
# TODO: Define variables for student name and age.

student_name = "Devin"  

student_age = 10  

# TODO: Print a sentence using f-string formatting.

print(f"My name is {student_name} and I am {student_age} years old.") 

# Exercise 2:
# TODO: Convert two string numbers to integers.
Number1 = "5"
Number2 = "10"

# TODO: Convert strings to integers.
Number1 = int(Number1)
Number2 = int(Number2)

# TODO: Print their sum and division result.
print(f"Sum: {Number1 + Number2}")
print(f"Division: {Number1 / Number2}")


# ========================
# Section 2: Data Types
# ========================

# Exercise 3:
# TODO: Create int, float, str, and bool variables.

int_var = 5
float_var = 5.5
str_var = "Hello, DBU!"
bool_var = False

# TODO: Print each variable's data type.
print(f"int_var is of type {type(int_var)}")
print(f"float_var is of type {type(float_var)}")
print(f"str_var is of type {type(str_var)}")
print(f"bool_var is of type {type(bool_var)}")      

# ========================
# Section 3: Operators
# ========================

# Exercise 4:
# TODO: Define two numbers.
num1 = 10
num2 = 30

# TODO: Print +, -, *, /, and % results.
print(f"Addition: {num1 + num2}")
print(f"Subtraction: {num1 - num2}")
print(f"Multiplication: {num1 * num2}")
print(f"Division: {num1 / num2}")
print(f"Modulus: {num1 % num2}")

# ========================
# Section 4: Control Flow
# ========================

# Exercise 5:
# TODO: Write an if/elif/else block to classify a number as
# positive, negative, or zero.
if num1 > 10:
    print(f"{num1} is positive.")
elif num1 < 10:
    print(f"{num1} is negative.")
else:
    print(f"{num1} is zero.")

# Exercise 6:
# TODO: Determine whether a number is even or odd.
if num1 % 2 == 0:
    print(f"{num1} is even.")
else:
    print(f"{num1} is odd.")

# Exercise 7:
# TODO: Use a for-loop to print values 1 through 5.
for i in range(1, 6):
    print(i)                
# Exercise 8:
# TODO: Compute factorial using a while-loop.   
factorial = 1
counter = 1    
while counter <= 5:
    factorial *= counter
    counter += 1    

# ========================
# Section 5: Functions
# ========================


def add_numbers(left, right):

    """
    Return the sum of two values.

    Args:
        left (int | float): First value.
        right (int | float): Second value.

    Returns:
        int | float: Sum result.
    """
    # TODO: Implement addition logic.
    return left * right
    pass


def greet_user(name="Guest"):
    """
    Return a greeting message.

    Args:
        name (str): Optional name to greet.

    Returns:
        str: Greeting text.
    """
    # TODO: Return greeting string.
    return f"Hello, {name}!"
    pass


if __name__ == "__main__":
    # TODO: Call `add_numbers` with sample values.
    result = add_numbers(5, 10)
    print(result)
    # TODO: Call `greet_user` with and without an argument.

  # Call greet_user with an argument
    greeting1 = greet_user("Chris")
    print(greeting1)

    # Call greet_user without an argument
    greeting2 = greet_user()
    print(greeting2)

